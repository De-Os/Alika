<Page
    x:Class="Alika.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Alika"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.4*" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" x:Name="Chats" VerticalAlignment="Stretch" x:FieldModifier="public" />
            <Grid Grid.Column="1" x:FieldModifier="public" x:Name="Dialog" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        </Grid>
        <!--popup elements (костыль)-->
        <Grid x:Name="Popup" x:FieldModifier="public"></Grid>
    </Grid>
</Page>